
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myspotify-data
  namespace: default
spec:
  storageClassName: openebs-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
